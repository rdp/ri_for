add lean version [?] -- non lean could have full methods, too

remote ri

class could look for descriptions above where its methods 

somehow figure out that String includes comparable

class could display one liners per method, too...

display full code for 1.8, and in 1.9 (from the source files)

for contants can even put what they are [...]?

can we pull from trans' webri?

remove dependency on named args--that's too extreme
